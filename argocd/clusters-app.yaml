apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ztp-clusters
  namespace: openshift-gitops
spec:
  project: default

  source:
    repoURL: https://github.com/jaganhpe/ocp-ztp.git
    targetRevision: main
    path: clusters

  destination:
    server: https://kubernetes.default.svc
    namespace: open-cluster-management

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
